#include<bits/stdc++.h>

using namespace std;

int main()
{
    int cases;
    scanf("%d", &cases);
    for(auto cc{1}; cc <= cases; cc++)
    {
        int W, H, L, U, R, D;
        scanf("%d %d %d %d %d %d", &W, &H, &L, &U, &R, &D);


        printf("Case #%d: %d\n", &cc);
    }
}